version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: flowchat-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: flowchat
      POSTGRES_PASSWORD: flowchat_dev
      POSTGRES_DB: flowchat
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U flowchat']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: flowchat-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
  redis-data:
